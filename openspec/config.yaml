# OpenSpec SDD Configuration — censo-api
# Auto-generated by sdd-init on 2026-07-01
context: |
  NestJS v11 + TypeORM + PostgreSQL + class-validator.
  Modular architecture: src/modules/<modulo>/, shared layer in src/common/.
  Package manager: pnpm v11.9.0.
  TypeScript with nodenext module resolution, ES2023 target.
  Database: single donacion_connection (schema: donacion, autoLoadEntities).
  Auth: JwtAuthGuard with Bearer token local decoding.
  Catalogs pattern: Entity in src/common/entities/, BaseService<Entity> extension.
  Auditing: idCreado/idActualizado int columns + ts_fecha_timestamp_ins/upd timestamps.
strict_tdd: true
testing:
  runner:
    command: pnpm test
    framework: jest
    config: inline in package.json (rootDir: src, testRegex: .*\.spec\.ts$)
  layers:
    unit:
      available: true
      tool: jest + ts-jest
    integration:
      available: true
      tool: '@nestjs/testing + jest'
    e2e:
      available: true
      tool: supertest + jest (config: test/jest-e2e.json)
  coverage:
    available: true
    command: pnpm test:cov
    directory: coverage/
  quality:
    linter:
      available: true
      command: pnpm lint
      tool: ESLint v9 + typescript-eslint (flat config)
    type_checker:
      available: true
      command: npx tsc --noEmit
      tool: TypeScript v5.7
    formatter:
      available: true
      command: pnpm format
      tool: Prettier v3 (singleQuote, trailingComma all)
phases:
  propose:
    required: true
  spec:
    required: true
  design:
    required: true
  tasks:
    required: true
  apply:
    required: true
  verify:
    required: true
  archive:
    required: true
delivery:
  strategy: ask-on-risk
  review_budget: 800
